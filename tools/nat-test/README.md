Usage: `.\NATTest.exe sync.milesthenerd.net`

Building:

```powershell
dotnet publish tools/nat-test/NATTest.csproj -c Release -r win-x64
dotnet publish tools/nat-test/NATTest.csproj -c Release -r linux-x64
```