{
    "name": "nattunnel-mediation-server",
    "version": "1.6.0",
    "private": true,
    "description": "NATTunnel mediation server — handles peer discovery, NAT detection, and introducer election for the NATTunnel mesh networking library.",
    "main": "server.js",
    "scripts": {
        "start": "node server.js"
    },
    "optionalDependencies": {
        "werift": "^0.20.0"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}
